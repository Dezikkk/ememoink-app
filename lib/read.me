lib/
 ├── ui/                                           // Warstwa prezentacji (UI)
 │    ├── core/                                    // Elementy UI wspólne dla całej aplikacji
 │    │     ├── ui/                                // Styl, widgety współdzielone
 │    │     │     ├── shared_widgets/              // Reużywalne widgety (przyciski, pola itp.)
 │    │     │     │     └── primary_button.dart
 │    │     │     └── themes/                      // Motywy — kolory, teksty, styl globalny
 │    │     │           ├── app_theme.dart
 │    │     │           └── colors.dart
 │    │     │
 │    │     └── view_model/                        // Globalne view modele aplikacji
 │    │           └── app_view_model.dart
 │    │
 │    ├── main/                                    // FEATURE: strona główna
 │    │     ├── view_model/
 │    │     │     └── main_view_model.dart         // Logika stanu dla Main Page
 │    │     └── widgets/
 │    │           └── main_page.dart               // UI strony głównej
 │    │
 │    ├── calendar/                                // FEATURE: kalendarz Google
 │    │     ├── view_model/
 │    │     │     └── calendar_view_model.dart     // Obsługa eventów, pobieranie z API
 │    │     └── widgets/
 │    │           ├── calendar_page.dart           // Widok kalendarza
 │    │           └── event_tile.dart              // Kafelek pojedynczego wydarzenia
 │    │
 │    ├── settings/                                // FEATURE: ustawienia
 │    │     ├── view_model/
 │    │     │     └── settings_view_model.dart     // Logika ustawień (motyw, konto itp.)
 │    │     └── widgets/
 │    │           └── settings_page.dart           // UI ekranu ustawień
 │    │
 │
 ├── domain/                                       // Warstwa domeny — czysta logika
 │    ├── models/                                  // Modele biznesowe (nie API!)
 │    │     ├── event.dart                         // Model eventu kalendarza
 │    │     └── user.dart                          // Model użytkownika
 │    │
 │    ├── repositories/                            // Kontrakty repozytoriów (abstrakcje)
 │    │     ├── calendar_repository.dart           // Interfejs dla kalendarza
 │    │     └── auth_repository.dart               // Interfejs dla autoryzacji Google
 │    │
 │    └── usecases/                                // Logika biznesowa
 │          ├── fetch_calendar_events.dart         // Pobranie eventów z kalendarza
 │          └── sign_in_with_google.dart           // Logowanie z Google OAuth
 │
 ├── data/                                         // Warstwa danych (API, modele API, implementacje)
 │    ├── model/                                   // Modele danych przychodzących z API
 │    │     ├── event_model.dart                   // Model API wydarzenia (JSON)
 │    │     └── user_model.dart                    // Model API użytkownika
 │    │
 │    ├── repositories/                            // Implementacje repozytoriów
 │    │     ├── calendar_repository_impl.dart      // Implementacja kalendarza
 │    │     └── auth_repository_impl.dart          // Implementacja logowania Google
 │    │
 │    ├── services/                                // WARSTWA API i integracji z zewnątrz
 │    │     ├── google_calendar_service.dart       // Połączenie z Google Calendar API
 │    │     └── google_auth_service.dart           // Obsługa OAuth (Google Sign-In)
 │    │
 │    └── api/                                     // Klienci HTTP / konfiguracja sieci
 │          └── google_api_client.dart             // Klient HTTP z autoryzacją OAuth
 │
 ├── config/                                       // Konfiguracje aplikacji
 │    ├── google_api_config.dart                   // Client ID, scopy, ustawienia Google API
 │    └── environment.dart                         // Konfiguracja środowisk: dev/staging/prod
 │
 ├── utils/                                        // Narzędzia / helpery
 │    ├── date_helper.dart                         // Formatowanie dat
 │    ├── logger.dart                              // Logger (debug pod aplikację)
 │    └── validators.dart                          // Walidacje formularzy
 │
 ├── routing/                                      // Nawigacja aplikacji
 │    └── app_router.dart                          // Generowanie ścieżek / routing
 │
 ├── main.dart                                     // Build w trybie produkcyjnym
 ├── main_development.dart                         // Build w trybie developerskim
 └── main_staging.dart                             // Build w trybie staging
